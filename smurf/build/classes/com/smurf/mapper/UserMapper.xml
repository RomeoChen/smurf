<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.smurf.mapper.UserMapper">
	    <insert id="add" parameterType="User" >
		insert into user ( name, sex, idNumber, originPlace, marriage , nation , birth , politicalStatus , residence , province , city , street , telephone )
	    values (#{name},#{sex},#{idNumber},#{originPlace},#{marriage},#{nation},#{birth},#{politicalStatus},#{residence},#{province},#{city},#{street},#{telephone}) 
	    </insert>
	    

	</mapper>

